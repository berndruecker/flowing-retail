# Default values for flowing-retail.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

nameOverride: "kafka-java-orchestrated-inventory"
fullnameOverride: ""

image:
  repository: "flowingretail/kafka-java-inventory"
  tag: latest
  pullPolicy: IfNotPresent
